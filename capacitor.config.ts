import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'capacitor-window-scroll',
  webDir: 'dist/capacitorWindowScroll',
  bundledWebRuntime: false
};

export default config;
